[build]
builder = "nixpacks"

[deploy]
startCommand = "uv run bridge.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
